#include "main.h"
#include <stdio.h>

/**
 * puts2 - prints every other character of a string
 * @str: the string in question
 * Return: Always 0.
 */

void puts2(char *str)
{
	int a = 0;

	for (a = 0; a != '\0')
	{
		_putchar(s[a]);
		a++;
		_putchar('\n');
	}
}
