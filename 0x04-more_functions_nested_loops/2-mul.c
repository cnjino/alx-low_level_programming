#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: interger number 1
 * @b: interger number 2
 * Return: Always 0.
 */

int mul(int a, int b)
{
	int a;
	int b;

	for (c = a * b)
	{
		_putchar(c);
	}
	return (0);
}
