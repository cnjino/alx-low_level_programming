#include "main.h"
#include <stdio.h>

/**
 * print_numbers - prints number from 0 to 9
 * Return: Always 0.
 */

void print_numbers(void)
{
	if (c >= 0 && c <= 9)
	{
		_putchar(c);
		c++;
	}
	{
		_putchar('\n');
	}
	return (0);
}

